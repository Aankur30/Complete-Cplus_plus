#include <iostream>
using namespace std;
int main()
{
    // not working since the optional is not a identifier in the compiler existing so we have to upgrade the version of the compiler then we will proceed.

    return 0;
}